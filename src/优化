2026-0707-1
修改完成！现在当用户在正压启动界面将警报切换为消音后，返回主页时，屏幕右上角会正确显示"消音"状态（黄色闪烁），而不是"警报"状态。

主要修改内容：

将主界面的警报显示逻辑与正压启动界面保持一致
添加了 muteOn 状态判断
当 muteOn 为 true 时，显示黄色闪烁的"消音"文字
当 muteOn 为 false 时，显示红色闪烁的"警报"文字
这样，消音状态会在整个系统得到保持，无论在哪个界面都会正确显示当前的消音状态。

2026-0707-2
主要修改内容：

将 mode==0 && globalAlarm 的条件判断分开
当 globalAlarm 为 true 时，正常显示警报或消音状态
当 globalAlarm 为 false 时，用 tft.fillRect(220,3,95,24,TFT_BLACK) 清除显示区域
这样，当压力从警报状态恢复到正常状态时，右上角的警报指示会完全消失，保持界面的整洁。

2026-0707-3
修改完成！现在当压力恢复正常时，无论在主界面还是正压启动界面，右上角的警报显示都会被清除。

主要修改内容：

修改了正压启动界面（mode=1）的显示逻辑
当 globalAlarm 为 true 时，正常显示警报或消音状态
当 globalAlarm 为 false 时，用 tft.fillRect(220,3,95,24,TFT_BLACK) 清除显示区域
这样，当压力从警报状态恢复到正常状态时，在正压启动模式下，右上角也会完全清除警报指示，不会再显示红色的"警报"字样。

2026-0707-4
当柜内压力达到预警值后触发警报，即使再次进入正压启动模式，警报状态也会保持不变，直到：

柜内压力恢复正常值范围，或者
用户手动点击"取消警报"按钮
这确保了警报状态的正确性和一致性

2020-0707-5
修改完成！现在的逻辑是：

设备通电后：

initialCheckDone = false
主界面不会检查压力状态
不会触发警报
进入正压启动并倒计时结束后：

initialCheckDone = true
主界面开始检查压力状态
可以触发警报
返回主页后：

initialCheckDone 保持为 true
主界面会继续检查压力状态
当压力达到警报阈值时，会显示警报
这样既满足了您"不进入正压启动就不触发警报"的要求，又能在进入过正压启动后，在主页正确显示警报状态。


08-15
1.优化了当柜内压力低于下限时，进行进气补充气体时，刚达到压力的下限值时就停止补充气体。
2.优化了正压启动逻辑

08-20
1.增加息屏功能
2.加入万能密码
3.优化了正压启动逻辑
